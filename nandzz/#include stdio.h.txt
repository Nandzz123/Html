#include <stdio.h>

int sum(int a[], int n)
{
    int i, s = 0;

    for (i = 0; i < n; i++)
        s = s + a[i];

    return s;
}

int main()
{
    int a[5] = {10, 20, 30, 40, 50};

    printf("sum=%d", sum(a, 5));

    return 0;
}
